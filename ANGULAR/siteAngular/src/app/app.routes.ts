import { Routes } from '@angular/router';
import { Login } from './components/login/login';

export const routes: Routes = [
    {path: "login", component: Login},
];
//Controla as rotas que a nossa apliação vai ter (Ex.: Home, Loja, Perfil, etc.)